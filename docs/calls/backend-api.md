# Backend API РґР»СЏ РІРёРґРµРѕР·РІРѕРЅРєРѕРІ (L3)

Р­С‚РѕС‚ РјРѕРґСѓР»СЊ СЂРµР°Р»РёР·СѓРµС‚ СЃРѕР·РґР°РЅРёРµ РєРѕРјРЅР°С‚, РіРµРЅРµСЂР°С†РёСЋ JWT, СЃС‚Р°С‚СѓСЃС‹ Рё Р»РѕРіРё. Р’СЃРµ СЌРЅРґРїРѕРёРЅС‚С‹ СЃРµР№С‡Р°СЃ РґРѕСЃС‚СѓРїРЅС‹ С‚РѕР»СЊРєРѕ Р°РґРјРёРЅР°Рј (РїСЃРёС…РѕР»РѕРіР°Рј) С‡РµСЂРµР· `x-auth-token` СЃ СЂРѕР»СЊСЋ `admin/super_admin` Рё СѓС‡РµС‚РѕРј Р°СЂРµРЅРґР°С‚РѕСЂР° (`x-practitioner-id`).

## РњРѕРґРµР»Рё Рё С‚Р°Р±Р»РёС†С‹
- `CallSessions { id, roomId, hostId, clientId, bookingId, practitionerId, startAt, expiresAt, status }`
- `JoinTokens { id, sessionId, userId, role, jwt, expiresAt }`
- `CallLogs { id, sessionId, userHash, event, timestamp, metadata }`
РРЅРґРµРєСЃС‹: `roomId`, `expiresAt`, `sessionId`, `practitionerId`, `timestamp`.

РњРёРіСЂР°С†РёСЏ: `backend/migrations/20251014090500-add-calls-tables.js`
РњРѕРґРµР»Рё: `backend/models/CallSession.js`, `backend/models/JoinToken.js`, `backend/models/CallLog.js`

## РџРµСЂРµРјРµРЅРЅС‹Рµ РѕРєСЂСѓР¶РµРЅРёСЏ (СЃРѕРІРјРµСЃС‚РЅРѕ СЃ L2)
- `JITSI_JWT_APP_SECRET` вЂ” СЃРµРєСЂРµС‚ РґР»СЏ HS256 (РґРѕР»Р¶РµРЅ СЃРѕРІРїР°РґР°С‚СЊ СЃ prosody/Jitsi РІ L2)
- `JITSI_JWT_APP_ID` (РёРЅС„Рѕ РґР»СЏ Jitsi; РґР»СЏ РїРѕРґРїРёСЃРё РІ РєРѕРґРµ РёСЃРїРѕР»СЊР·СѓРµС‚СЃСЏ SECRET)
- `JITSI_JWT_ISS` вЂ” issuer (РїРѕ СѓРјРѕР»С‡Р°РЅРёСЋ `psyform-backend`)
- `JITSI_JWT_AUD` вЂ” audience (РїРѕ СѓРјРѕР»С‡Р°РЅРёСЋ `psyform-calls`)
- `JITSI_XMPP_DOMAIN` вЂ” `sub` РєР»РµР№Рј, РїРѕ СѓРјРѕР»С‡Р°РЅРёСЋ `meet.jitsi`
- `PUBLIC_URL` вЂ” Р±Р°Р·РѕРІС‹Р№ URL Jitsi Web (РЅР°РїСЂРёРјРµСЂ, `https://jitsi.localhost:8443`)
- `ENABLE_CALLS_SWEEP` вЂ” РІРєР»СЋС‡Р°РµС‚ РїРµСЂРёРѕРґРёС‡РµСЃРєСѓСЋ РѕС‡РёСЃС‚РєСѓ РїСЂРѕСЃСЂРѕС‡РµРЅРЅС‹С… СЃРµСЃСЃРёР№ (РїРѕ СѓРјРѕР»С‡Р°РЅРёСЋ `true`)

> РќР°СЃС‚СЂРѕР№С‚Рµ СЌС‚Рё РїРµСЂРµРјРµРЅРЅС‹Рµ РІ РѕРєСЂСѓР¶РµРЅРёРё backend (РЅР°РїСЂРёРјРµСЂ, РІ `.env` РёР»Рё С‡РµСЂРµР· Docker Compose) С‚Р°Рє, С‡С‚РѕР±С‹ РѕРЅРё СЃРѕРІРїР°РґР°Р»Рё СЃ `.env.calls` (L2).

## Р­РЅРґРїРѕРёРЅС‚С‹

### POST `/api/calls`
РЎРѕР·РґР°РµС‚ СЃРµСЃСЃРёСЋ РґР»СЏ Р·Р°РїРёСЃРё Рё РіРµРЅРµСЂРёСЂСѓРµС‚ host/guest СЃСЃС‹Р»РєРё.

РўРµР»Рѕ:
```json
{ "bookingId": "<uuid>", "ttlMinutes": 90 }
```
Р—Р°РіРѕР»РѕРІРєРё: `x-auth-token` (admin), `x-practitioner-id` (РѕР±СЏР·Р°С‚РµР»СЊРЅРѕ).
РћС‚РІРµС‚:
```json
{
  "id": "<uuid>",
  "roomId": "pf-...",
  "expiresAt": "2025-10-14T...Z",
  "status": "created",
  "hostUrl": "https://jitsi.localhost:8443/room?jwt=...",
  "guestUrl": "https://jitsi.localhost:8443/room?jwt=...",
  "ttlMinutes": 90
}
```

### GET `/api/calls/:id/invite/candidates`
Р’РѕР·РІСЂР°С‰Р°РµС‚ Р±Р»РёР¶Р°Р№С€РёРµ Р·Р°РїРёСЃРё (в€’24С‡ вЂ¦ +30РґРЅРµР№) РґР»СЏ С‚РµРєСѓС‰РµРіРѕ РїСЃРёС…РѕР»РѕРіР°, С‡С‚РѕР±С‹ Р±С‹СЃС‚СЂРѕ РІС‹Р±СЂР°С‚СЊ РїРѕР»СѓС‡Р°С‚РµР»СЏ РїСЂРёРіР»Р°С€РµРЅРёСЏ РёР· РєРѕРјРЅР°С‚С‹.

РћС‚РІРµС‚:
```json
{
  "rows": [
    {
      "id": "<bookingId>",
      "clientName": "РРјСЏ",
      "slotTime": "2025-10-14T10:00:00.000Z",
      "sourceTimezone": "Europe/Moscow",
      "clientId": "<uuid|null>",
      "telegramUsername": "handle",
      "telegramUserId": "123456789",
      "tgChatId": "123456789"
    }
  ]
}
```

### POST `/api/calls/:id/invite`
РћС‚РїСЂР°РІР»СЏРµС‚ РїСЂРёРіР»Р°С€РµРЅРёРµ РєР»РёРµРЅС‚Сѓ С‡РµСЂРµР· Telegram-Р±РѕС‚Р°.

РўРµР»Рѕ:
```json
{ "bookingId": "<uuid>" }
```
Р›РёР±Рѕ `{ "clientId": "<uuid>" }` вЂ” РµСЃР»Рё РЅСѓР¶РЅР° РѕС‚РїСЂР°РІРєР° РєРѕРЅРєСЂРµС‚РЅРѕРјСѓ РєР»РёРµРЅС‚Сѓ РІРЅРµ РєРѕРЅС‚РµРєСЃС‚Р° Р·Р°РїРёСЃРё.

РџРѕРІРµРґРµРЅРёРµ:
- Р“РµРЅРµСЂРёСЂСѓРµС‚ (РёР»Рё РїРµСЂРµРёСЃРїРѕР»СЊР·СѓРµС‚) РіРѕСЃС‚РµРІРѕР№ С‚РѕРєРµРЅ Рё СЃСЃС‹Р»РєСѓ.
- Р•СЃР»Рё Р·Р°РґР°РЅ `PUBLIC_WEB_URL`, С‚Рѕ СЃСЃС‹Р»РєР° С„РѕСЂРјР°С‚Р° `/calls/:roomId?jwt=...&d=<jitsiHost>&sid=<sessionId>&exp=<iso>` (web-СЃС‚СЂР°РЅРёС†Р° СЃ С‚Р°Р№РјРµСЂРѕРј, pre-join, Р»РѕРіР°РјРё С…РѕСЃС‚Р°).
- РРЅР°С‡Рµ вЂ” РїСЂСЏРјР°СЏ Jitsi-СЃСЃС‹Р»РєР° `PUBLIC_URL/<room>?jwt=...`.
- Р”Р»СЏ СѓРґРѕР±СЃС‚РІР° Р·Р°РїРёСЃС‹РІР°РµС‚ СЃСЃС‹Р»РєСѓ РІ `Booking.meetLink` (РµСЃР»Рё РїРµСЂРµРґР°РЅ `bookingId`).
- РџРёС€РµС‚ СЃРѕР±С‹С‚РёРµ `invite_sent` РІ `CallLogs`.

### POST `/api/calls/:id/close`
Р—Р°РєСЂС‹РІР°РµС‚ СЃРµСЃСЃРёСЋ (СЃС‚Р°С‚СѓСЃ `ended`) Рё РїРёС€РµС‚ СЃРѕР±С‹С‚РёРµ `session_closed` РІ `CallLogs`.

### GET `/api/calls/:id/status`
Р’РѕР·РІСЂР°С‰Р°РµС‚ СЃРѕСЃС‚РѕСЏРЅРёРµ СЃРµСЃСЃРёРё: `{ id, roomId, status, startAt, expiresAt }`.

### POST `/api/calls/:id/log`
Р—Р°РіР»СѓС€РєР° РґР»СЏ Р·Р°РїРёСЃРё СЃРѕР±С‹С‚РёР№ (РЅР°РїСЂРёРјРµСЂ, `participantJoined/Left`). РўРµР»Рѕ:
```json
{ "event": "participantJoined", "metadata": {"samples":1} }
```
РРґРµРЅС‚РёС„РёРєР°С‚РѕСЂ РїРѕР»СЊР·РѕРІР°С‚РµР»СЏ Р±РµСЂС‘С‚СЃСЏ СЃ СЃРµСЂРІРµСЂР° РёР· Р°СѓС‚РµРЅС‚РёС„РёС†РёСЂРѕРІР°РЅРЅРѕР№ СЃРµСЃСЃРёРё (`req.user.id`) Рё С…РµС€РёСЂСѓРµС‚СЃСЏ РїРѕ `CALL_LOG_SALT` (env) в†’ `userHash`.

## TTL Рё Р±РµР·РѕРїР°СЃРЅРѕСЃС‚СЊ
- TTL РѕРіСЂР°РЅРёС‡РµРЅ в‰¤ 90 РјРёРЅСѓС‚. РўРѕРєРµРЅС‹ `exp` РІС‹СЃС‚Р°РІР»СЏСЋС‚СЃСЏ РїРѕ `expiresAt`.
- РџРµСЂРёРѕРґРёС‡РµСЃРєРёР№ РїСЂРѕС†РµСЃСЃ РїРѕРјРµС‡Р°РµС‚ РїСЂРѕСЃСЂРѕС‡РµРЅРЅС‹Рµ СЃРµСЃСЃРёРё РєР°Рє `expired` (`worker.js` РІС‹Р·С‹РІР°РµС‚ `scheduleExpirySweep`).
- Р›РѕРіРё РЅРµ СЃРѕРґРµСЂР¶Р°С‚ РџР”РЅ вЂ” `userId` в†’ `userHash`.

### РћРіСЂР°РЅРёС‡РµРЅРёРµ Р»РѕРіРѕРІ РіРѕСЃС‚СЏ
РќР° С‚РµРєСѓС‰РµРј СЌС‚Р°РїРµ С‚РѕР»СЊРєРѕ С…РѕСЃС‚ (Р°РґРјРёРЅ) РїРёС€РµС‚ СЃРѕР±С‹С‚РёСЏ С‡РµСЂРµР· `/api/calls/:id/log`. Р“РѕСЃС‚РµРІС‹Рµ РєР»РёРµРЅС‚С‹ РЅРµ Р°РІС‚РѕСЂРёР·РѕРІР°РЅС‹ РЅР° Р±СЌРєРµРЅРґРµ Рё РёС… СЃРѕР±С‹С‚РёСЏ РЅРµ Р·Р°РїРёСЃС‹РІР°СЋС‚СЃСЏ. Р­С‚Рѕ РѕРіСЂР°РЅРёС‡РµРЅРёРµ MVP; РјРѕР¶РЅРѕ РѕСЃС‚Р°РІРёС‚СЊ Рё Р·Р°РґРѕРєСѓРјРµРЅС‚РёСЂРѕРІР°С‚СЊ, Р»РёР±Рѕ СЂРµР°Р»РёР·РѕРІР°С‚СЊ Р»С‘РіРєСѓСЋ РіРѕСЃС‚РµРІСѓСЋ Р°РІС‚РѕСЂРёР·Р°С†РёСЋ РїРѕР·РґРЅРµРµ.

## РљР°Рє Р·Р°РїСѓСЃС‚РёС‚СЊ РјРёРіСЂР°С†РёРё (Р»РѕРєР°Р»СЊРЅРѕ)
1. РЈР±РµРґРёС‚РµСЃСЊ, С‡С‚Рѕ backend РІРёРґРёС‚ Postgres (СЃРј. РІР°С€ `docker-compose.yml`, СЃРµСЂРІРёСЃ `db` СЃР»СѓС€Р°РµС‚ `localhost:5433`).
2. Р—Р°РїРѕР»РЅРёС‚Рµ `.env` backend (РёР»Рё РїРµСЂРµРјРµРЅРЅС‹Рµ РѕРєСЂСѓР¶РµРЅРёСЏ) вЂ” `DATABASE_URL`, `JITSI_JWT_*`, `PUBLIC_URL`.
3. Р’С‹РїРѕР»РЅРёС‚Рµ РјРёРіСЂР°С†РёРё:
```powershell
# РёР· РєР°С‚Р°Р»РѕРіР° backend
npm run migrate
```

## РџСЂРёРјРµСЂС‹ JWT (HS256)
Payload (РјРёРЅРёРјР°Р»СЊРЅРѕ):
```json
{
  "aud": "psyform-calls",
  "iss": "psyform-backend",
  "sub": "meet.jitsi",
  "room": "pf-abc123",
  "exp": 1893456000,
  "context": { "user": { "role": "host" } }
}
```
РџРѕРґРїРёСЃС‹РІР°РµС‚СЃСЏ СЃРµРєСЂРµС‚РѕРј `JITSI_JWT_APP_SECRET`. Р­С‚Рё С‚РѕРєРµРЅС‹ backend РіРµРЅРµСЂРёСЂСѓРµС‚ Р°РІС‚РѕРјР°С‚РёС‡РµСЃРєРё РїСЂРё `POST /api/calls`.

## РўСЂРµР±РѕРІР°РЅРёСЏ Рє PUBLIC_WEB_URL
Р•СЃР»Рё РІС‹ С…РѕС‚РёС‚Рµ, С‡С‚РѕР±С‹ РїСЂРёРіР»Р°С€РµРЅРёРµ РѕС‚РїСЂР°РІР»СЏР»Рѕ СЃСЃС‹Р»РєСѓ РЅР° РІРµР±-СЃС‚СЂР°РЅРёС†Сѓ `/calls/:roomId` (СЃ С‚Р°Р№РјРµСЂРѕРј, pre-join, Рё РѕС‚РєР»СЋС‡С‘РЅРЅС‹Рј deep-link), Р·Р°РґР°Р№С‚Рµ:

- `PUBLIC_WEB_URL` вЂ” РїСѓР±Р»РёС‡РЅС‹Р№ URL С„СЂРѕРЅС‚РµРЅРґР° (РЅР°РїСЂРёРјРµСЂ, `http://localhost:3001` Р»РѕРєР°Р»СЊРЅРѕ).
- `PUBLIC_URL` вЂ” Р±Р°Р·РѕРІС‹Р№ URL Jitsi Web (РЅР°РїСЂРёРјРµСЂ, `https://jitsi.localhost:8443`).

Р’ СЌС‚РѕРј СЃР»СѓС‡Р°Рµ РІ СЃСЃС‹Р»РєРµ Р±СѓРґСѓС‚ РїР°СЂР°РјРµС‚СЂС‹ `jwt`, `d` (С…РѕСЃС‚ Jitsi), `sid` (id СЃРµСЃСЃРёРё), `exp` (ISO). Р•СЃР»Рё `PUBLIC_WEB_URL` РЅРµ Р·Р°РґР°РЅ, Р±РѕС‚ РѕС‚РїСЂР°РІРёС‚ РїСЂСЏРјСѓСЋ Jitsi-СЃСЃС‹Р»РєСѓ.
